import 'package:flutter/material.dart';

class CatchPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Catch Page')), 
      body: Center(child: Text('Catch Page Content')), 
    );
  }
}
